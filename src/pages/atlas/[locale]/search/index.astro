---
/** サイト内検索（Pagefind）。ビルド後に生成されるインデックスを利用する */
import AtlasLayout from "../../../../layouts/AtlasLayout.astro";
import Breadcrumb from "../../../../components/Breadcrumb.astro";
import { atlasPath, withBase } from "../../../../lib/site";
import {
  localizedName,
  t,
  LOCALES,
  localeNames,
  type Locale,
} from "../../../../lib/i18n";
import { getPublishedSubjects } from "../../../../lib/content";

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;

/** 選択した分野に応じてカテゴリを絞り込むための対応表。 */
const categorySubjects: Record<string, string[]> = {};
for (const subject of await getPublishedSubjects()) {
  const subjectName = localizedName(subject.data.name, locale);
  for (const category of subject.data.categories) {
    const categoryName = localizedName(category.name, locale);
    categorySubjects[categoryName] = [
      ...(categorySubjects[categoryName] ?? []),
      subjectName,
    ];
  }
}

/** Pagefindのフィルタ名 → ローカライズ済みラベル */
const filterLabels: Record<string, string> = {
  subject: t(locale, "subjects"),
  category: t(locale, "category"),
  locale: t(locale, "languages"),
};
---

<AtlasLayout
  locale={locale}
  title={t(locale, "search")}
  description={locale === "ja"
    ? "学習サイト「アトラス」の記事を分野横断で検索します。"
    : "Search articles across all subjects."}
  noindex={true}
  localeSwitchHrefs={Object.fromEntries(
    LOCALES.map((l) => [l, atlasPath(l, "search")]),
  )}
>
  <div class="container">
    <Breadcrumb
      items={[
        { label: t(locale, "breadcrumbHome"), href: atlasPath(locale) },
        { label: t(locale, "search") },
      ]}
    />
    <h1>{t(locale, "search")}</h1>
    <form
      class="search-form"
      role="search"
      data-search-form
      data-base={withBase("/")}
      data-locale-name={localeNames[locale]}
      data-all-label={t(locale, "all")}
      data-filter-labels={JSON.stringify(filterLabels)}
      data-category-subjects={JSON.stringify(categorySubjects)}
    >
      <label for="search-input">{t(locale, "searchPlaceholder")}</label>
      <div class="search-row">
        <input
          id="search-input"
          type="search"
          data-search-input
          placeholder={t(locale, "searchPlaceholder")}
          autocomplete="off"
        />
        <button type="submit" class="button">{t(locale, "search")}</button>
      </div>
      <div class="search-filters" data-search-filters>
        {/* フィルタはPagefindのインデックスから動的生成される */}
      </div>
    </form>
    <p class="search-note" data-search-note>
      {
        locale === "ja"
          ? "検索インデックスはビルド後に生成されます（npm run build）。開発サーバーでは検索できません。"
          : "The search index is generated at build time (npm run build)."
      }
    </p>
    <div data-search-status aria-live="polite" class="visually-hidden"></div>
    <p class="search-count" data-search-count aria-live="polite"></p>
    <ol class="search-results" data-search-results></ol>
  </div>
</AtlasLayout>

<style>
  .search-form {
    max-width: var(--reading-width);
  }
  .search-row {
    display: flex;
    gap: var(--space-2);
  }
  .search-row input {
    flex: 1;
  }
  .search-filters {
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
    margin-top: var(--space-2);
  }
  .search-filters :global(label) {
    font-weight: 400;
    color: var(--text-secondary);
  }
  .search-filters :global(select[multiple]) {
    min-width: 12rem;
    min-height: 5.5rem;
  }
  .search-note {
    color: var(--text-secondary);
    font-size: calc(0.9rem * var(--text-scale));
  }
  .search-results {
    list-style: none;
    margin: var(--space-4) 0 0;
    padding: 0;
    max-width: var(--reading-width);
  }
  .search-count {
    min-height: 1.5em;
    margin: var(--space-3) 0 0;
    color: var(--text-secondary);
  }
  .search-results :global(li) {
    padding-block: var(--space-3);
    border-bottom: 1px solid var(--border-default);
  }
  .search-results :global(mark) {
    background: var(--accent-subtle);
    color: var(--accent-primary);
    font-weight: 700;
  }
  .search-results :global(.result-title) {
    font-weight: 600;
  }

  /* iOS Safari は 16px 未満の入力欄で画面を拡大してしまう */
  @media (max-width: 767px) {
    #search-input {
      font-size: max(16px, calc(0.9rem * var(--text-scale)));
    }
  }
</style>

<script>
  import { createPagefindAdapter, type SearchAdapter } from "../../../../lib/search";

  const form = document.querySelector<HTMLFormElement>("[data-search-form]");
  const input = document.querySelector<HTMLInputElement>("[data-search-input]");
  const resultsEl = document.querySelector<HTMLElement>(
    "[data-search-results]",
  );
  const filtersEl = document.querySelector<HTMLElement>(
    "[data-search-filters]",
  );
  const note = document.querySelector<HTMLElement>("[data-search-note]");
  const status = document.querySelector<HTMLElement>("[data-search-status]");
  const count = document.querySelector<HTMLElement>("[data-search-count]");

  let adapter: SearchAdapter | null = null;

  // 現在ページのロケール表示名（例: 日本語 / English）と各種ラベル
  const localeName = form?.dataset.localeName ?? "";
  const allLabel = form?.dataset.allLabel ?? "---";
  let filterLabels: Record<string, string> = {};
  try {
    filterLabels = JSON.parse(form?.dataset.filterLabels ?? "{}");
  } catch {
    filterLabels = {};
  }
  let categorySubjects: Record<string, string[]> = {};
  try {
    categorySubjects = JSON.parse(form?.dataset.categorySubjects ?? "{}");
  } catch {
    categorySubjects = {};
  }

  const filterOrder = ["subject", "category", "locale"];

  function selectedValues(select: HTMLSelectElement): string[] {
    return [...select.selectedOptions]
      .map((option) => option.value)
      .filter(Boolean);
  }

  function normalizeMultipleSelect(select: HTMLSelectElement): void {
    if (!select.multiple) return;
    const selected = [...select.selectedOptions];
    const all = selected.find((option) => option.value === "");
    if (all) {
      for (const option of select.options) option.selected = option === all;
    } else if (selected.length === 0 && select.options[0]) {
      select.options[0].selected = true;
    }
  }

  function refreshCategoryOptions(
    available: Record<string, Record<string, number>>,
  ): void {
    const subjectSelect = filtersEl?.querySelector<HTMLSelectElement>(
      'select[data-filter-name="subject"]',
    );
    const categorySelect = filtersEl?.querySelector<HTMLSelectElement>(
      'select[data-filter-name="category"]',
    );
    if (!subjectSelect || !categorySelect) return;

    const subjects = selectedValues(subjectSelect);
    const selectedCategories = new Set(selectedValues(categorySelect));
    const categories = Object.entries(available.category ?? {}).filter(
      ([category]) =>
        subjects.length === 0 ||
        (categorySubjects[category] ?? []).some((subject) =>
          subjects.includes(subject),
        ),
    );
    categorySelect.replaceChildren();
    const all = document.createElement("option");
    all.value = "";
    all.textContent = allLabel;
    categorySelect.append(all);
    for (const [value, total] of categories) {
      const option = document.createElement("option");
      option.value = value;
      option.textContent = `${value} (${total})`;
      option.selected = selectedCategories.has(value);
      categorySelect.append(option);
    }
    normalizeMultipleSelect(categorySelect);
  }

  async function init(): Promise<void> {
    if (!form) return;
    try {
      adapter = await createPagefindAdapter(form.dataset.base ?? "/");
      note?.remove();
      // フィルタUIを動的生成
      const available = await adapter.filters();
      if (filtersEl) {
        const names = [
          ...filterOrder.filter((name) => available[name]),
          ...Object.keys(available).filter((name) => !filterOrder.includes(name)),
        ];
        for (const name of names) {
          const values = available[name];
          if (!values) continue;
          const id = `filter-${name}`;
          const wrap = document.createElement("span");
          const label = document.createElement("label");
          label.htmlFor = id;
          label.textContent = filterLabels[name] ?? name;
          const select = document.createElement("select");
          select.id = id;
          select.dataset.filterName = name;
          select.multiple = name === "subject" || name === "category";
          const all = document.createElement("option");
          all.value = "";
          all.textContent = allLabel;
          select.append(all);
          for (const [v, count] of Object.entries(values)) {
            const opt = document.createElement("option");
            opt.value = v;
            opt.textContent = `${v} (${count})`;
            select.append(opt);
          }
          // 言語フィルタは現在ページのロケールを初期選択にする
          if (name === "locale" && localeName && values[localeName]) {
            select.value = localeName;
          }
          select.addEventListener("change", () => {
            normalizeMultipleSelect(select);
            if (name === "subject") refreshCategoryOptions(available);
            void run();
          });
          wrap.append(label, select);
          filtersEl.append(wrap);
        }
        refreshCategoryOptions(available);
      }
    } catch {
      /* 開発サーバーではインデックスがないため案内文を残す */
    }
  }

  async function run(): Promise<void> {
    if (!adapter || !input || !resultsEl) return;
    const q = input.value.trim();
    if (!q) {
      resultsEl.innerHTML = "";
      if (count) count.textContent = "";
      if (status) {
        status.textContent =
          document.documentElement.lang === "ja"
            ? "検索語を入力してください"
            : "Enter a search term";
      }
      return;
    }
    const filters: Record<string, string | string[] | undefined> = {};
    filtersEl
      ?.querySelectorAll<HTMLSelectElement>("select")
      .forEach((s) => {
        filters[s.dataset.filterName ?? ""] = s.multiple
          ? selectedValues(s)
          : s.value;
      });
    const items = await adapter.search(q, filters);
    resultsEl.innerHTML = "";
    for (const item of items) {
      const li = document.createElement("li");
      const a = document.createElement("a");
      a.className = "result-title";
      a.href = item.url;
      a.textContent = item.title;
      const p = document.createElement("p");
      // 本文由来の抜粋には数式の読み上げ文字列や内部IDが混ざるため、
      // 編集済みの要約を検索結果の説明として使う。
      p.textContent = item.meta.summary ?? "";
      li.append(a, p);
      resultsEl.append(li);
    }
    const resultCount =
      document.documentElement.lang === "ja"
        ? `${items.length}件の記事`
        : `${items.length} results`;
    if (count) count.textContent = resultCount;
    if (status) status.textContent = resultCount;
  }

  // Pagefind は動的 import なので、回線や端末によってはページ表示直後の
  // 最初の送信が初期化より先に到達する。初期化完了を待ってから検索することで、
  // 「検索を押したのに結果が空のまま」という競合を防ぐ。
  const adapterReady = init();

  form?.addEventListener("submit", (ev) => {
    ev.preventDefault();
    void adapterReady.then(run);
  });
  input?.addEventListener("input", () => {
    if ((input?.value.length ?? 0) >= 2) void adapterReady.then(run);
  });

  // ヘッダーの検索欄から ?q= 付きで来たときは、そのまま結果まで出す
  void adapterReady.then(() => {
    const q = new URL(location.href).searchParams.get("q");
    if (q && input) {
      input.value = q;
      void run();
    }
  });
</script>
